# Keystone offline Stage 0 result

Date: 2026-08-27

Disposition: passed with zero model/provider calls

## What qualified

The fresh fourteen-source world is exact and task-locked. On the deterministic
permitted ingress path, the ordinary packet reaches 23,021 prompt tokens after
ten sources, 2,029 above the 20,992-token allowance. Externalizing the first
eligible source observation restores feasibility at 19,495 tokens. This is an
activation argument based on realized allowed reads, not total accessible
world size.

Both provider-free lifecycle fixtures construct the same 1,542-word,
fourteen-source artifact and enter verification. Its first current check fails
exactly `K04_power`. The same stale section-bound repair is rejected twice,
with exact POWER observations arriving after each failure. V0 then exposes
neither unresolved rejection nor recurrence. V1 retains the rejection and
records recurrence count two.

A common 521-token candidate-/artifact-/section-bound alternative repair is
admitted. Its effect crosses a later decision boundary, clears the rejected
epoch, and makes the old check stale. The next current check passes. The exact
candidate is independently classified ready in the frozen fixture, and
submission mechanics complete. Verification packets remain below 6,500 tokens.

Four relationship reversals—track run count, route-proving count, power
capacity addition, and telemetry coverage—are detected by the frozen
candidate-bound evaluator.

## Claim limits

This proves task custody, pressure geometry, action transport, replacement
laws, evaluator sensitivity, and provider-free reachability. It does not show
that Qwen will create the artifact, repeat or recover from a rejected repair,
use V1's causal facts, improve readiness, or close correctly.

The next eligible operation is the separately authorized treatment-free
Keystone pressure screen. No measured V0/V1 runner and no GPU authority exist.
