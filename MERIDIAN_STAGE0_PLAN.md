# Meridian source-local delta × direct exact-work Stage 0

Date: 2026-08-25

Status: offline Stage 0 implemented and provider-free qualified; authentic
pressure remains unobserved; no provider or GPU call is authorized

## Whole-system question

Under authentic result-delivery pressure, does a source-local semantic
transform mechanically merged into exact source/version slots produce more
source-correct cumulative work, current verification, and appropriate closure
than direct actor-authored exact work?

The measured unit will be the complete pressure-to-work-to-effect-to-check-to-
repair-to-recheck-to-closure trajectory. Delta expression alone is not a
utility outcome.

## Fresh task and non-oracular world

Meridian is a new sterile-infusion manufacturing and allocation task. Sixteen
deterministically generated sources use opaque alphabetic IDs rather than a
source/requirement number mapping. Twelve requirements form a many-to-many
graph held by the evaluator but not supplied to the actor.

The exact world includes traps for:

- alert versus rejection thresholds and their units;
- probability versus humidity, prevalence, or readiness;
- registry coverage versus demand;
- nominal line totals versus a shared validated sterilizer constraint;
- contract targets versus observed supplier and fuel performance;
- a temperature range versus one collapsed decimal;
- overlapping clinical cohorts;
- source, lot, and candidate version currency;
- stale checks after component, line, route, shift, or candidate effects; and
- mechanical completion versus independent readiness.

## Complete configurations

| Function | W0_DIRECT_WORK | L1_LOCAL_DELTA |
|---|---|---|
| Actor-selected exact ingress | yes | yes |
| Exact custody, reopen, and positive-savings relief | yes | yes |
| Exact source evidence register | actor owns one-slot upserts | maintenance delta is merged one source/version slot at a time |
| Incremental decision artifact | actor owned | actor owned |
| Candidate effects/currentness | exact | exact |
| Check, repair, recheck, closure | same surface and actor budget | same surface and actor budget |
| External quality/readiness | identical | identical |

L1 maintenance calls are additional provider, token, cache, and latency cost.
They do not replace ordinary actor decisions. W0 is not receipt-only: it has a
legal exact source-slot work action.

Both paths enforce the same 650-token per-source slot ceiling with the frozen
tokenizer. Every admitted slot records the exact result IDs whose source bytes
previously crossed that arm's model boundary. W0 therefore cannot write work
for an unread source, and L1 does not receive a provenance privilege that W0
lacks. The treatment distinction is who performs the bounded semantic
transformation and the additional maintenance cost—not evidence access,
provenance, slot size, artifact surface, or readiness authority.

## Narrow semantic authority

An L1 call receives only exact source observations that have just been
externalized by the common pressure policy and the prior slots for those same
sources. It must emit exactly one bounded block per current source. The host
validates source ID, source SHA-256, total/per-slot token budgets, local
headings, current-batch coverage, source allowlist, and absence of closure
language.

The merge key is `(source_id, source_version)`. A new accepted block replaces
only that source slot. Unrelated slots remain byte-identical. The model cannot
rewrite a complete global state, fill unavailable requirements, edit an absent
source, or authorize readiness.

## Exact offline geometry

The complete source corpus is 66,457 bytes and 23,730 tokenizer tokens. Every
full source and all 120 full two-source pairs fit the permitted ingress action;
the largest pair is 3,634 model-visible result tokens and 9,555 exact source
bytes.

A prospective ordinary path reaches ten delivered sources across ten domains
at 20,192 prompt tokens. The next exact two-source result makes the packet
23,628 tokens, 2,636 over the 20,992 prompt allowance. Replacing only the
oldest positive-savings result body restores feasibility at 20,774 tokens.

This is an activation opportunity, not evidence that the actor will choose the
same path. The live screen must produce authentic model-driven pressure after
at least four fully covered sources across three domains, with a novel pending
source result, unchanged candidate, no check, and no submission.

## Provider-free lifecycle qualification

Both complete configurations were executed without a provider call. Each
externalized five two-source observations and produced ten exact source slots.
W0 used ten actor-owned slot effects; L1 used five separately counted
maintenance effects. In both:

- every slot retained exact input-result provenance and stayed within the
  common 650-token ceiling;
- unrelated source slots remained byte-identical across updates;
- a synthetic exact source-version successor replaced one slot without
  changing other slots;
- a defect-bearing decision failed a candidate-bound check;
- repair made that check stale;
- the recheck passed the mechanical precheck and was current;
- submission remained a proposal requiring independent adjudication; and
- the final 1,514-word fixture passed all mechanical gates but returned
  `not_adjudicated`, never `ready`.

These fixtures establish apparatus reachability, not model utility.

## Resource and stop contract

The future measured fork gives each arm up to 32 preconstruction actor calls.
Once a frozen mechanical construction milestone is reached, exactly ten more
actor decisions are protected for effect uptake, check, repair, recheck, and
closure. L1 may use at most twelve separately accounted maintenance calls.

The immediate live operation is only the common 24-call ordinary pressure
screen. It stops on authentic eligible pressure, candidate mutation, check,
submission, call/token/wall budget, ineligible pressure, or apparatus failure.
One attempt is allowed per call and retries are zero.

If the screen qualifies, its exact boundary is sealed before any treatment is
rendered. Source-local live expression is then separately qualified from that
boundary. The measured continuation is not authorized or frozen by Stage 0.

## Claim limits

- Offline size is not authentic pressure.
- Provider-free lifecycle success is not model capability or task utility.
- A local delta may still omit or distort meaning despite passing mechanical
  provenance validation.
- Exact source slots may anchor error as well as preserve insight.
- A positive L1 trajectory would support the complete local configuration,
  not an isolated delta-provider effect.
- No task, prompt, budget, cadence, or evaluator may be tuned after live
  behavior.
