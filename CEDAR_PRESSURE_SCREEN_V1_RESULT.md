# Cedar ingress-aligned pressure screen v1 result

Run ID: `2026-08-25-cedar-ingress-aligned-pressure-screen-v1`

Authorized freeze:
`952037afa399c0fd593605831d92f8988a81d4a4`

Disposition: **passed authentic interaction boundary**. The run used five
actor/provider calls, one attempt per call, zero retries, and 57,444 serialized
tokens. The model server was released and the run tree is sealed.

## Literal trajectory

The actor made five valid two-source batch reads, each covering lines 1–70:

1. S01–S02;
2. S03–S04;
3. S05–S06;
4. S07–S08; and
5. pending S09–S10.

All five responses stopped normally and contained accepted actions. There were
no malformed or rejected actions, exact reopens, candidate mutations, checks,
or submissions.

The prompt grew from 3,266 to 19,665 tokens over the five actor calls. Adding
the exact pending `RESULT-005` would make the next ordinary prompt 23,376
tokens against the frozen 20,992-token allowance: a 2,384-token deficit.

## Ingress-aligned activation

Before pending delivery, S01–S08 had each delivered all 70 lines across prior
actor decision boundaries. They span six frozen evidence domains:

- authority;
- hazard;
- demand;
- transport;
- shelter/care; and
- communications.

The frozen minimum was four sources at 55/70 unique lines across three domains.
The boundary therefore exceeds rather than merely touches the maturity gate.
Pending S09–S10 contains 140 novel lines. The candidate remains exact-initial,
and no check or submission occurred. The audit reports no eligibility failure.

## Positive common pressure trigger

With pending `RESULT-005` protected, the deterministic positive-savings scan
selects only the oldest eligible result, `RESULT-001`. Replacing its exact body
with its receipt reduces the same packet to 19,881 tokens, leaving 1,111 tokens
of prompt headroom while preserving the 4,096-token response reserve.

Thus the exact fork supplies all prerequisites for the whole-system comparison:

```text
distributed evidence crossed actor boundaries
        +
novel exact evidence is pending
        +
ordinary delivery is physically blocked
        +
one reversible common relief operation restores feasibility
        +
candidate and closure state remain untouched
```

## Claim limit and next gate

This screen qualifies one common pre-treatment fork. It does not run semantic
maintenance, artifact coupling, continuation, evaluation, or closure and
therefore establishes no D0/A1 utility result.

`CEDAR_PRESSURE_SCREEN_AUDIT.json` passes, the exact handoff is materialized,
and `MEASURED_PREFLIGHT.json` passes for the complete detached-then-coupled
continuation. The measured run remains GPU-unauthorized and requires a new
clean result/freeze commit and exact owner authorization.
