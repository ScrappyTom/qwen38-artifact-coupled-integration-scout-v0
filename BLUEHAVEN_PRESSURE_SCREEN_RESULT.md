# Bluehaven ordinary pressure-screen result

Date: 2026-08-25

Status: authentic common pressure boundary passed, independently audited, and
sealed; no B1/W1 continuation or maintenance call authorized by this result

Frozen screen commit:
`87cc19ea18bd5b24fe3ab9707176bc21e2b7e764`

Run ID: `2026-08-25-bluehaven-pressure-screen-v0`

## Literal result

The ordinary actor made six provider calls, each with one attempt and no
retry. Every response was a valid two-range `read_batch` action:

| Call | Exact requested sources | Prompt tokens |
|---:|---|---:|
| 1 | S01 + S02 | 3,309 |
| 2 | S03 + S04 | 6,629 |
| 3 | S05 + S06 | 10,198 |
| 4 | S07 + S08 | 13,557 |
| 5 | S09 + S10 | 16,948 |
| 6 | S11 + S12 | 20,246 |

Calls 1–5 delivered complete 70-line source ranges for S01–S10 across ten
frozen evidence domains. Call 6 acquired a novel 140-line S11/S12 batch as
`RESULT-006`. Adding that exact result to the next ordinary invocation would
produce a 23,820-token prompt against the frozen 20,992-token prompt
allowance: an authentic 2,828-token result-delivery overflow.

The candidate remained exactly initial, no check ran, and no submission was
proposed. The actor therefore reached pressure during distributed evidence
ingress rather than after construction or through a malformed action.

## Protected-pending first-fit counterfactual

The screen did not apply treatment. Its frozen host-only eligibility check
protected pending `RESULT-006`, scanned prior exact visible source results in
the declared order, and found one strictly positive substitution:

```text
ordinary pending-result packet       23,820 tokens
replace RESULT-001 body with receipt
feasible packet after relief          20,917 tokens
prompt allowance                      20,992 tokens
remaining prompt headroom                 75 tokens
```

The result therefore qualifies one exact common pre-treatment fork. It does
not show that the future B1 or W1 policy is useful, that maintenance output is
expressible, that either actor will construct, or that either final candidate
will be ready.

## Audit and custody

The independent offline audit passed with no failures. It verified:

- the run seal and exact freeze/authorization identity;
- six provider attempts, one per call, and zero retries;
- exact prompt-token recount for all six calls and the pressure packet;
- exact assistant-response hashes and usage arithmetic;
- deterministic reexecution of all six source observations;
- unchanged candidate bytes;
- source-line/domain activation rather than result-object counting;
- one protected-pending positive first-fit substitution; and
- GPU/server release with no live runtime left behind.

The run consumed 71,166 serialized prompt-plus-completion tokens. The exact
handoff remains marked `measured_fork_authorized: false`. Maintenance
expression qualification and any B1/W1 continuation require new frozen
apparatus and separate explicit authorization.

## Scientific disposition

Bluehaven passes the Stage 0 activation gate on the model's actual ingress
path. The realized path also closely matches the prospective path without
being forced: six sequential full two-source batches reached mature pressure
with no treatment active.

This is apparatus and boundary evidence only. The next scientific question
remains configuration-level interaction:

```text
B1: three positive externalizations → one coupled exact-ledger rewrite
    + ordinary decision work + current verification/repair/closure

W1: no maintenance provider
    + actor-owned exact ledger and decision work
    + the same current verification/repair/closure loop
```

The common exact prefix removes pre-pressure behavioral divergence from that
comparison. It does not predetermine what either arm will retain, request,
construct, verify, or close after `RESULT-006` is admitted.
