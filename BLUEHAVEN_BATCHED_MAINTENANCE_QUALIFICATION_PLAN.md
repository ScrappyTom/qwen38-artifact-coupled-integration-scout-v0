# Bluehaven batched-maintenance expression qualification

Date: 2026-08-25

Status: frozen offline; two model calls require separate exact authorization;
no B1/W1 continuation is authorized

## Purpose

The live screen qualified a common authentic pressure fork. Before B1 can be
measured as a complete operating policy, its distinctive provider operation
must show that it can express one bounded complete ledger replacement through
the live model/runtime transport. This qualification is deliberately small:
it does not optimize wording and it does not score downstream task utility.

## Exact cases

`Q1_INITIAL_THREE_RESULT_BATCH` supplies exact `RESULT-001` through
`RESULT-003` (S01–S06) with no prior integrated ledger.

`Q2_REPLACEMENT_THREE_RESULT_BATCH` supplies a frozen bounded prior grounded in
S01–S06 plus exact `RESULT-004` through `RESULT-006` (S07–S12). It tests
replacement rather than unbounded append.

Both case inputs come from the sealed ordinary pressure history. The grouping
qualifies the B1 carrier; it is not represented as an already observed live
maintenance event. In the eventual measured policy, a maintenance batch may
form only from three results that the first-fit lifecycle actually
externalizes.

## Admission and stop rule

Each call receives a 2,700-token provider completion allowance. A result is
accepted only if it finishes normally, begins with
`# Evidence Integration Ledger`, contains at most 2,400 tokenizer tokens,
references only its exact allowlisted sources, and contains no submission or
readiness authorization.

There is one attempt per call and no retry. A rejection remains a qualified
negative transport result; it does not authorize prompt repair or a measured
run. Both cases must pass before B1/W1 continuation becomes eligible for its
own separately frozen and authorized run.

## Claim limits

Passing would establish only bounded expression through this exact interface.
It would not establish semantic fidelity, economic value, downstream use,
artifact quality, verification quality, or readiness. Failing would close
this frozen B1 carrier as presently specified; it would not invalidate W1,
the common pressure substrate, or semantic maintenance in all forms.
