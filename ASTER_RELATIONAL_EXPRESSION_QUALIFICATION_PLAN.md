# Aster boundary-native relational-expression qualification

Date: 2026-08-26

Status: completed after one separately authorized call; raw semantic material
was locally safe, exact-line transport failed, and W0/L1 continuation remains
unauthorized

## Why this exact case

The common pressure screen selected the case mechanically. Six ordinary actor
reads created an exact pending-result overflow. Frozen first-fit relief
externalizes only `RESULT-001`, containing ANCHOR and BRIDGE. The one possible
first L1 investment is therefore a provenance-local relational replacement for
those exact owner sources.

The investigator did not choose a later or more favorable source pair. The
case uses the sealed result bytes and versions, the authoritative task for
relevance only, and an empty current provenance register.

## One-call gate

The maintenance model must emit the frozen plain-text carrier with one to four
claims for each of ANCHOR and BRIDGE, no more than eight claims and 1,500 total
tokens. Each claim copies one exact unique complete source line from
`RESULT-001`. The host derives line numbers and span hashes.

A relationship may name source objects literally present in the owner evidence
and statement. This does not create their slots or attest to their current
authoritative state. Cross-source synthesis is forbidden in the carrier and
remains exact actor-authored candidate work.

Mechanical admission establishes shape, exact result/version/span custody,
mutation scope, referent binding, currentness, and budgets. The prospectively
frozen safety contract separately reviews authority, units, timing, predicate,
modality, stale/current status, and readiness. Omission is recorded, not
silently repaired; each owner source must still contribute at least one
materially relevant claim.

## Interpretation

Passing establishes only that Qwen can express one bounded, mechanically
admissible, materially safe provenance-local relational delta at the first
authentic source-externalization boundary. It does not establish persistence,
actor uptake, substitution for exact recovery, artifact benefit, readiness, or
positive economics.

The observed call failed and therefore closes this exact Aster carrier route at
this boundary. There is no same-boundary prompt, schema, budget, allowlist, or
retry ladder. See `ASTER_RELATIONAL_EXPRESSION_QUALIFICATION_RESULT.md`.

The complete W0/L1 continuation would have required a separate freeze and
authorization even if this call had passed. Because the gate failed, that
continuation remains closed.
