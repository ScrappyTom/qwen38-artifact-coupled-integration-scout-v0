# Orchard phase-lifecycle measured-run freeze

Date: 2026-08-27

Status: offline preflight passed; zero new provider calls; measured GPU run not
authorized

## Exact starting boundary

Both cells hydrate the audited live pressure handoff from run
`2026-08-27-orchard-phase-lifecycle-pressure-screen-v0`:

* six actor decisions and six valid two-source acquisitions;
* ten complete sources already delivered;
* COMMUNE/CHANGE pending as `RESULT-006`;
* 21,152 ordinary prompt tokens, 160 above allowance;
* deterministic externalization of `RESULT-001` only;
* 18,509 tokens after relief and 2,483 tokens of headroom;
* unchanged initial candidate; and
* no prior treatment, semantic maintenance, or construction.

The exact source/result ledger, candidate, message history, run seal, pressure
audit, and handoff hashes are frozen inputs. The measured runner cannot select a
different relevance packet.

## Complete compared systems

This is a compound interaction comparison, not component isolation.

Before the actor crosses the frozen mechanical construction milestone, F0 and
P1 share:

* the same exact pressure state and first-fit relief;
* the same fallible anchored-provenance maintenance on newly externalized source
  observations;
* the same per-claim admission, bounded register, and unchanged-state fallback;
* the same exact evidence matrix and section/patch/global artifact actions;
* the same actor and maintenance seeds, action grammar, source access, effect
  uptake, and resource policy; and
* the same actor-selected `begin_verification` transition, admissible only after
  the exact decision has the frozen title/headings, at least 900 words, and at
  least seven source citations.

After that event:

```text
F0_FIXED_SCAFFOLD_APPEND_ONLY_VERIFICATION
  retains the frozen semantic scaffold
  retains exact current task work
  appends verification actions, effects, and checks
  uses first-fit relief only when required

P1_PHASE_CONDITIONAL_CURRENT_VERIFICATION
  externalizes the semantic scaffold behind an exact handle
  retains exact current task work
  replaces the model-facing verification frame after every action
  binds current candidate, latest effect, check currency, pending result,
  exact reopen handles, and full-history handle
```

The comparison intentionally does not isolate scaffold demotion from mechanical
verification-state replacement. It tests whether that whole phase-conditional
allocation improves the construction-to-verification-to-closure lifecycle.

## Resource and stop contract

The frozen order is F0 then P1. Each cell permits at most 24 construction actor
calls, 12 verification actor calls, 36 actor calls total, and 12 maintenance
calls. Across both cells the hard limits are 72 actor calls, 24 maintenance
calls, and 96 provider calls. Each call has one attempt and zero retries.

The maintenance operation is available only during construction. Failure to
cross the construction milestone by the construction budget is a measured
terminal disposition, not a forced transition. Verification ends on submission,
its 12-call limit, another declared resource limit, unrelievable pressure, or an
apparatus failure. Budget exhaustion does not trigger progress prompting or
forced closure.

## Offline qualification

The provider-free preflight passes and binds:

* the exact audited handoff and relief geometry;
* the inert authorization request and absent measured run directory;
* the 72/24/96 call limits;
* the earlier complete F0/P1 check/repair/stale-check/recheck/submission fixtures;
* four relationship-reversal traps; and
* the rule that common semantic maintenance is not conditioned on configuration
  identity before verification.

The offline fixtures prove apparatus reachability, not live model behavior. They
do not predict that the actor will construct, invoke the phase transition, use
the scaffold well, repair correctly, or close.

## Main interpretation targets

The measured run must be evaluated as a lifecycle vector:

* when semantic claims are admitted and how the register saturates;
* when exact task construction starts and which action granularity is used;
* whether both cells remain identical before the transition;
* whether the actor crosses the mechanical phase boundary;
* effect uptake and current/stale check discrimination;
* defect discovery, exact repair, current recheck, readiness, and closure;
* prompt/completion/cache/latency and maintenance cost;
* relief/reopen behavior and failure migration; and
* candidate-bound external artifact quality.

An F0/P1 difference would support only the full post-transition lifecycle
configuration in this world. It would not identify scaffold demotion or current-
state replacement as the individual cause.

## Authorization boundary

The repository contains an inert authorization request. This freeze authorizes
no GPU call. A live run requires the exact commit-bound statement emitted after
the repository is clean, tested, committed, and pushed.
