# Aster common pressure-screen result

Date: 2026-08-26

Run ID: `2026-08-25-aster-provenance-relational-pressure-screen-v0`

Frozen commit: `f91fdaff28b2c7ad760afa90877b284e26529814`

Status: authentic interaction-aligned pressure boundary passed; relational
expression and W0/L1 continuation remain unmeasured and unauthorized

## Result

The ordinary actor made six valid full two-source batch reads with one attempt
per call and zero retries:

1. ANCHOR + BRIDGE
2. CIRRUS + DUSK
3. EMBER + FORGE
4. GROVE + HARBOR
5. IRIS + JUNIPER
6. KELP + LATTICE

The first five results crossed later actor decision boundaries. They cover ten
complete sources across ten host-side evidence domains. `RESULT-006` remained
newly pending and contains 128 novel lines.

Adding the pending result to ordinary chronology produced 21,787 prompt
tokens. The allowance is 20,992 prompt tokens after the fixed 4,096-token
response reserve, so the authentic delivery deficit was 795 tokens.

The frozen protected-pending positive-savings scan selects only `RESULT-001`.
Replacing that exact ANCHOR+BRIDGE result body with its reopenable receipt
produces an 18,951-token packet. The pending KELP+LATTICE observation can
therefore be delivered with 2,041 prompt tokens of spare room.

## Validity

- Six provider calls completed normally.
- Every action parsed and was admitted.
- Every action was a full two-source batch read.
- No treatment or semantic-maintenance call occurred.
- The candidate remained at exact initial SHA-256
  `e5af16cba812e1b73e8153b3ff67ee40f61a82b97f9062a9e8ef5b64f5051836`.
- No check or submission occurred.
- The six calls consumed 66,362 serialized tokens.
- Exact result reexecution, offline prompt recount, first-fit replay,
  activation reconstruction, provider receipts, tree seal, runtime assets,
  authorization, and GPU/runtime release all passed independent audit.
- No retry, budget stop, or run failure exists.

The actor-selected source sequence differs slightly from the fixed offline
opportunity path, but reaches the same structural event after six batches: ten
delivered sources, one novel pending pair, and first-fit externalization of
`RESULT-001`. This is authentic actor-driven activation, not replication of
the provider-free path.

## Systems interpretation

The screen deliberately allowed candidate work and checks before pressure; it
did not require an acquisition-only history. Qwen nevertheless chose six
successive evidence batches and left the artifact unchanged. This makes the
observed fork an acquisition-state boundary, but that state is endogenous to
the ordinary actor rather than imposed by the host.

The exact treatment opportunity is now fixed by lifecycle behavior:

```text
ten exact sources already delivered
+ KELP/LATTICE pending and protected
+ ordinary packet cannot fit
+ RESULT-001 ANCHOR/BRIDGE is the first-fit source externalization
+ both W0 and L1 remain otherwise identical and viable
```

The screen is common history, not a no-relief control and not evidence for the
semantic mechanism. Its value is selecting the first possible L1 investment
without investigator relevance judgment.

## What this establishes

- authentic, distributed, actor-selected source pressure;
- exact pending-result protection;
- one positive, feasible, source-externalizing first-fit operation;
- an exact common W0/L1 fork;
- sufficient mechanical headroom for the pending observation; and
- clean custody, replay, sealing, and resource release.

## What this does not establish

The run does not show that:

- Qwen can express the provenance-local relational carrier;
- the carrier preserves ANCHOR/BRIDGE meaning;
- the register will substitute for later exact recovery;
- the ordinary actor will use the register;
- either system will construct, verify, repair, or close correctly;
- L1 improves artifact quality or readiness; or
- either configuration has positive information-economic value.

## Routing

The exact fork is bound in `ASTER_PRESSURE_BOUNDARY_HANDOFF.json`. The next
eligible design step is offline: render and freeze one expression case over the
exact ANCHOR+BRIDGE `RESULT-001` selected by the observed first-fit transition.
A live expression call requires a new clean commit and separate authorization.

Passing that gate would establish only bounded provenance-local expression and
frozen material safety at this boundary. The complete W0/L1 continuation would
still require its own runner, commit, and authorization, and interpretation
must follow induced demand through task work, effects, current checks, repair,
closure, cost, and independent readiness.
