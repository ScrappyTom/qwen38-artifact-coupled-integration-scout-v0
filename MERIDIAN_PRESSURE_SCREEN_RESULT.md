# Meridian common pressure-screen result

Date: 2026-08-25

Run ID: `2026-08-25-meridian-source-local-delta-pressure-screen-v0`

Frozen commit: `467ccc0d032af217b969a31315ee41005dbe2167`

Status: authentic common pressure boundary passed; source-local expression and
W0/L1 continuation remain unmeasured and unauthorized

## Result

The ordinary actor made six valid full two-source batch reads with one attempt
per call and zero retries:

1. AXIOM + BRAMBLE
2. CIPHER + DRIFT
3. EMBER + FJORD
4. GLINT + HEATH
5. IRIS + JASPER
6. KNOLL + LOOM

The first five results crossed later actor decision boundaries. They cover ten
complete sources across ten host-side evidence domains. `RESULT-006` remained
newly pending and contributed 140 novel lines.

Adding that pending result to ordinary chronology produced a 23,628-token
prompt. The allowance is 20,992 prompt tokens after the fixed 4,096-token
response reserve, so the authentic delivery deficit was 2,636 tokens.

The frozen protected-pending positive-savings scan selects only `RESULT-001`.
Replacing that exact result body with its reopenable receipt produces a
20,774-token packet. The pending KNOLL+LOOM observation can therefore be
delivered with 218 prompt tokens of spare room.

## Validity

- Six provider calls completed normally.
- Every action parsed and was admitted.
- Every action was a full two-source batch read.
- No treatment or maintenance call occurred.
- The candidate remained at exact initial SHA-256
  `a5adf105f06aa57e26868a4fae8422e2383ffba1bc57e50a72a077c3e534cac1`.
- No check or submission occurred.
- The six calls consumed 69,784 serialized tokens.
- Exact result reexecution, offline prompt recount, first-fit replay, activation
  reconstruction, provider receipts, tree seal, runtime assets, authorization,
  and GPU/runtime release all passed independent audit.
- No retry, budget stop, or run failure exists.

The actor-selected trajectory exactly matched the prospective six-batch
geometry from offline Stage 0. That agreement strengthens the activation
qualification but is not an independent replication: it is one seed in one
fresh task world.

## What this establishes

The Meridian system now has one exact, mature, common pre-treatment fork at
which:

```text
distributed exact evidence is already delivered
+ one novel exact result is pending
+ ordinary chronology cannot carry it
+ one reversible positive-savings substitution can carry it
+ task work/check/closure has not begun
```

This is the intended interaction activation state. The earlier offline path
was only reachability; this run establishes authentic actor-driven pressure.

## What this does not establish

The run does not show that:

- W0 direct source-slot work is useful;
- L1 can express a grounded source-local delta;
- a delta will be used by the actor;
- either system will construct, verify, repair, or close correctly;
- semantic persistence will substitute for later exact recovery; or
- either configuration has positive information-economic value.

The screen is common history, not a control arm. Comparing relief with a hard
capacity stop would answer only operability, which is already established.

## Routing

The exact fork is bound in `MERIDIAN_PRESSURE_BOUNDARY_HANDOFF.json`. The next
eligible design step is offline: render and freeze the first actual L1
maintenance event created by this boundary—source-local work over the exact
AXIOM+BRAMBLE `RESULT-001` that first-fit externalizes. A live expression call
requires a new clean commit and separate authorization.

Passing that gate would establish only bounded, source-admissible expression.
The complete W0/L1 continuation would still need its own runner, commit, and
authorization, and its outcome must follow induced demand through artifact
effects, current checking, repair, closure, cost, and independent readiness.
