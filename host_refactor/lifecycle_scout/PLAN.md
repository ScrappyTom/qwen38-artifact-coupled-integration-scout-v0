# E97 Verification-Lifecycle Scout Plan

1. Verify the sealed E96 donor checkpoint, exact candidate, counters, domain snapshot, and terminal event.
2. Import the preterminal event state under a new execution manifest with an append-only migration receipt.
3. Apply E97 mechanically and prove that RESULT-013 through RESULT-017 are externalized, RESULT-018 remains pending, exact custody is unchanged, and the packet fits.
4. Freeze the candidate-bound `not_ready` adjudication and its blocking criteria before live behavior. Preserve exact raw evaluator output externally while bounding the actor-visible projection.
5. Provider-free qualify pending-effect delivery, incremental completion of the sixth section, verification entry, failed current check, bounded repair, current recheck, and closure.
6. Qualify exact checkpoint/resume after six additional actor calls and cumulative/marginal accounting.
7. Freeze the live runner and authorization request, run the full regression suite, commit, and push.
8. Stop for explicit GPU authorization.
