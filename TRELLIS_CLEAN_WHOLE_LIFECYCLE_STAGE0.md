# E105 — Trellis clean whole-lifecycle Stage 0

Date: 2026-08-30

Status: provider-free qualified and frozen for a separately authorized first
review tranche. No GPU/provider call occurred.

## What is being run

This is a new trajectory from the original Trellis task seed. It does not
resume E103 and imports none of E103's candidate bytes, checks, or event state.

The one configuration is:

```text
V1_CLEAN_PROSPECTIVE_LIFECYCLE

exact external custody
+ deterministic first-fit relief and exact reopen
+ fallible anchored scaffold during acquisition/construction
+ exact incremental task artifacts
+ corrected section-boundary transport
+ E97 delivered applied-effect turnover
+ E104 delivered-check turnover
+ exact pending effects
+ current candidate-bound verification
+ bounded section repair
+ independent external readiness adjudication
```

The scaffold is mechanically demoted when verification begins. It cannot
authorize readiness or closure.

## Provider-free full lifecycle

The exact checkpointed fixture completed in two tranches:

| Measure | Result |
|---|---:|
| Actor calls | 19 |
| Maintenance calls | 7 |
| Provider attempts | 26 |
| Serialized tokens | 343,119 |
| Pressure-relief events | 7 |
| Check sequence | fail → pass |
| Applied candidate effects externalized | 3 |
| Delivered failed checks externalized | 1 |
| Final ordered decision headings | 6/6 |
| Hidden/glued heading corruption | absent |
| Final frozen external readiness | ready |
| Submission | completed |

The complete repository regression is 333 passed. Focused Ruff passes for all
new lifecycle, builder, runner, and test files. Repository-wide Ruff and mypy
continue to expose pre-existing historical debt outside this Stage 0; no new
E105 lint error remains.

The initial candidate is independently `not_ready`. The final readiness rule is
external and candidate-bound; submission and model-authored state are not
readiness evidence.

This fixture proves that the complete host path is reachable after exact
checkpoint hydration. It does not establish how Qwen will pace acquisition,
whether its scaffold will preserve the right relationships, whether it will
repair correctly, or whether it will propose closure appropriately.

## First live tranche

The frozen runner begins from the same clean initial state and stops after at
most:

- 12 actor calls;
- 6 maintenance calls;
- 18 total provider calls;
- 400,000 serialized tokens;
- one attempt per call and zero retries.

It always stops for review at the tranche boundary or any earlier terminal.
Continuation is not automatic. Review must include literal actor and
maintenance transcripts, visible state, acquisition/reopen behavior, candidate
effects, verification currency, artifact quality, and failure migration.

## Disposition

Stage 0 passes. The live runner is frozen but not authorized. A separate exact
commit-bound GPU authorization is required.
