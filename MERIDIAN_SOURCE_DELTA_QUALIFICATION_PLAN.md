# Meridian first source-local delta expression qualification

Date: 2026-08-25

Status: frozen offline; one model call requires separate exact authorization;
no W0/L1 continuation is authorized

## Purpose

The common screen established the first real pressure event. Its frozen
positive-savings rule externalizes exactly `RESULT-001` so the pending
KNOLL+LOOM result can fit. `RESULT-001` contains exact full-source bytes for
AXIOM and BRAMBLE and had already crossed an ordinary actor boundary.

This gate asks whether the frozen source-local maintenance interface can
express the first actual L1 maintenance event. It does not invent a later batch,
reuse Bluehaven's global replacement cases, or compare task utility.

## Exact case

`Q1_FIRST_ACTUAL_EXTERNALIZATION` receives:

- the exact authoritative Meridian task;
- exact `RESULT-001` from the sealed pressure history;
- the exact empty source register from candidate version 000;
- exact AXIOM and BRAMBLE source SHA-256 bindings; and
- no other source result or global semantic state.

Seed 427032, prompt serialization, model/runtime, 1,800-token completion
allowance, 1,500-token total admission ceiling, and 650-token per-source
ceilings are frozen before inference.

## Qualification

Mechanical admission requires a normal stop and exactly one bounded block for
AXIOM and BRAMBLE. IDs, versions, headings, source scope, token budgets,
register markers, and closure language are validated literally.

After the run, the exact output receives a frozen material-safety review. The
review may record omissions but rejects material reversals, fabricated values,
wrong units or authority, stale-version promotion, probability conversion, or
self-authorized readiness. The output is never repaired or retried.

Mechanical transport and semantic safety are separate dispositions. Both must
qualify before the complete L1 arm is eligible. Passing does not show that the
actor uses the delta or that L1 improves artifacts, verification, closure, or
cost.

## Why one call

Only one L1 maintenance event is determined by the observed boundary. A second
replacement or different-source case would be investigator-authored rather
than generated by the frozen lifecycle. Version replacement and unrelated-slot
preservation already pass provider-free apparatus tests. Their live utility
belongs inside the complete feedback trajectory if those events arise.

## Stop rule

One attempt, zero retries. Any provider, carrier, source-scope, budget, or
material-safety failure closes this frozen expression interface for this route.
No same-boundary prompt, budget, or allowlist repair follows automatically.
