# Solace anchored-provenance pressure-screen result

Date: 2026-08-26

Run ID: `2026-08-26-solace-anchored-provenance-pressure-screen-v0`

Frozen commit: `5af42ca96182ce16dc5aced20f952da9a7c791e4`

Status: authentic interaction-aligned pressure boundary passed; no semantic
maintenance or measured continuation was authorized or run

## Literal result

The ordinary actor made six valid two-source batch reads, one attempt per call
and zero retries:

1. AURORA + BASTION
2. CIPHER + DELTA
3. ECHO + FALCON
4. GARNET + HELIX
5. INDIGO + JASPER
6. KESTREL + LUMEN

The first five observations crossed later actor decision boundaries. They cover
ten complete sources and ten declared evidence domains. `RESULT-006` is newly
pending and contains the complete 128-line KESTREL/LUMEN batch.

Delivering that pending result through ordinary chronology would require 21,211
prompt tokens. The allowance is 20,992 after the fixed 4,096-token response
reserve, so the exact deficit is 219 tokens.

The frozen protected-pending, positive-savings first-fit scan selects only
`RESULT-001`. Replacing its exact AURORA/BASTION body with the exact reopen
receipt yields an 18,595-token packet and 2,397 tokens of prompt headroom.

The candidate remained at its exact initial SHA-256
`9a228857dd50e69921af6d67c22e74bd3156dfb6266475ce63838881a822da61`.
No construction, check, submission, treatment, or maintenance operation
occurred. The six calls consumed 63,731 serialized tokens.

## Independent audit

The offline auditor passed with zero failures. It independently verified:

- authorization and frozen commit binding;
- all six provider receipts and one-attempt/no-retry execution;
- exact action re-execution and result-ledger reconstruction;
- offline prompt recount and token arithmetic;
- activation and candidate-state reconstruction;
- protected-pending first-fit selection and resulting feasibility;
- run-tree seal and runtime-asset custody; and
- clean model-server/GPU release.

The reusable Aster auditor was parameterized without changing its prior claim.
All 22 pressure-screen audit tests, including legacy Aster evidence, passed.

## Systems meaning

This is an authentic actor-selected acquisition state, not an investigator-
selected relevance packet. The actor had access to exact incremental work
actions throughout but chose six evidence batches and left both task artifacts
unchanged. The lifecycle transition now supplies a prospective treatment event:

```text
ten exact sources delivered
+ KESTREL/LUMEN newly pending and protected
+ ordinary chronology overflows by 219 tokens
+ RESULT-001 AURORA/BASTION is mechanically externalized first
+ exact anchor/context custody can operate on that externalized result
```

The pressure screen establishes only that this complete-system comparison can
activate cleanly. It says nothing about whether a provenance register will be
populated, whether relational claims will survive, whether the actor will use
them, or whether either arm will produce better exact work.

## Next eligible experiment

There will be no standalone expression gate. The next route is one fresh
whole-system W0/L1 continuation from this common fork:

- `W0_DIRECT_EXACT_WORK_FRESH`: first-fit relief plus ordinary exact work;
- `L1_FAULT_TOLERANT_ANCHORED_PROVENANCE`: the same system plus one charged,
  fallible semantic-maintenance call whenever newly externalized source
  observations become available.

In L1, each claim is admitted independently. The model chooses the semantic
statement, assertion mode, referents, and exact anchor substring. The host
materializes exact anchor/context spans and custody bindings. Rejected claims
are discarded without repair or retry; zero-valid output leaves the prior
register unchanged; ordinary actor work continues in every case.

The comparison must continue through induced reads, exact artifact mutations,
effect uptake, candidate-bound checks, repair/recheck opportunity, readiness,
closure, and full information cost. This result and its handoff authorize none
of those GPU calls.
