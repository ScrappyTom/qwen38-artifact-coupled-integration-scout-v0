# Keystone event-triggered causal continuation result

## Disposition

The frozen run is valid, but it is **non-diagnostic for the V0/V1 causal-continuity treatment**. The exact event trigger never occurred, so no branch was created and no treatment-dependent model decision was made.

Under the prospectively frozen stop rule, Keystone is closed rather than routed into another acquisition gate, retry, fresh task, or continuation.

## What happened

The runner restored the exact E79 parent once, applied common positive-savings first-fit relief, delivered the pending CYBER result, retained the common semantic-maintenance and incremental-artifact systems, and continued ordinary actor-selected ingress.

The common continuation used 18 new model calls:

- 10 semantic-maintenance calls;
- 8 actor calls;
- 198,745 serialized tokens;
- 517.359 seconds wall time.

The actor read the five previously unobserved sources—TELEMETRY, LOADS, PASSENGER, LINEAGE, and REVIEW—so the exact ledger ultimately contained all 14 Keystone sources. It still did not begin artifact construction. It then attempted one invalid 172-line batch read of MANDATE and TRACK, followed by separate complete rereads of those two already observed sources.

The candidate remained the initial 10-word placeholder, the phase remained `construction`, and no candidate-bound check, rejected repair, later observation, or causal fork existed.

## Corrected activation economics

The historical run's embedded summary contains a derived-reporting defect. It retained the detector's initial `empty_trace` snapshot because the terminal detector was only recomputed inside verification, and its activation-tax helper counted actor calls but omitted semantic-maintenance model calls.

The sealed traces support the corrected whole pre-treatment totals:

- parent actor calls: 9;
- common actor calls: 8;
- common semantic-maintenance calls: 10;
- total model calls before treatment: **27**;
- parent serialized tokens: 102,009;
- common actor serialized tokens: 161,037;
- common semantic-maintenance serialized tokens: 37,708;
- total serialized tokens before treatment: **300,754**;
- treatment-dependent decisions: **0**.

This defect does not alter the raw responses, event traces, candidate state, branch count, terminal disposition, release evidence, or tree seals. The run was not repeated. The runner and activation-tax helper were corrected for future use, and the correction is regression-tested.

## Scientific conclusion

The complete common system did not naturally expose the rejected-repair continuity boundary in Keystone. Acquiring every source object was not sufficient to cause construction or verification. Therefore this run provides no evidence for or against V1 causal continuity.

The useful result is routing discipline: common infrastructure remained active, the exact functional trigger governed branching, and the system stopped when the scientific interaction did not occur. No replacement eligibility screen is warranted.

The donor-preserving writable product profile is separate and unaffected.
